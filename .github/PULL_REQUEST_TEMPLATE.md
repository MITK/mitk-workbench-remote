## Summary

What does this pull request change, and why?

## Related issues

Closes #

## Checklist

- [ ] Formatted and linted (`ruff format src/ tests/`, `ruff check src/ tests/`)
- [ ] Type checks pass (`mypy --strict src/`)
- [ ] Tests pass (`pytest`) and new tests cover the change
- [ ] Documentation updated for any public API change
- [ ] `contract/openapi.json` updated if the REST API contract changed

---

This repository is mirrored from GitLab (`git.dkfz.de`), which is the single
source of truth. A maintainer integrates approved pull requests on the GitLab
side; the result then mirrors back to GitHub. See `CONTRIBUTING.md` for details.
